[build]
builder = "NIXPACKS"

[deploy]
startCommand = "sh -c 'alembic upgrade head || echo Skipping migration; uvicorn app.main:app --host 0.0.0.0 --port $PORT'"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
